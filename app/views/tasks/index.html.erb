<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <% if user_signed_in? %>
    <%= link_to '新しいタスクを追加', new_task_path %>
    <%= link_to 'タスクの管理', delete_tasks_path %>
  <% else %>
    <p>Please log in to create task</p>
  <% end %>

  <ul>
    <% @tasks.each do |task| %>
      <li>
        <strong><%= task.title %></strong>
        <%= render partial: "tasks/index", locals: { task: task } %>
      </li>
    <% end %>
  </ul>

</body>
</html>
