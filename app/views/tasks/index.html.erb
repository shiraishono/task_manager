<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>タスク一覧</title>
</head>

<body>
  <h1>タスク一覧</h1>
  
  <!-- タスク投稿フォーム -->
  <h2>新しいタスクを追加</h2>
  <%= form_with model: Task.new, url: tasks_path, local: true do |form| %>
    <div>
      <%= form.label :title, 'タスク名' %>
      <%= form.text_field :title %>
    </div>
    <div>
      <%= form.label :user_id, 'ユーザーID' %>
      <%= form.number_field :user_id %>
    </div>
    <div>
      <%= form.label :parent_task_id, '親タスクID' %>
      <%= form.number_field :parent_task_id %>
    </div>
    <div>
      <%= form.submit '追加' %>
    </div>
  <% end %>
  
  <h1>タスク一覧</h1>
  <ul>
    <% if @tasks.any? %>
      <% @tasks.each do |task| %>
        <li>タスク名: <%= task.title %>, ユーザーID: <%= task.user_id %>, 親タスクID: <%= task.parent_task_id %></li>
      <% end %>
    <% else %>
      <li>タスクがありません。</li>
    <% end %>
  </ul>
</body>
</html>